---
type Props = {
  href: string;
  label: string;
};

const { href, label } = Astro.props;
---

<a class="mb-5 inline-block text-sm underline-offset-4 hover:underline" href={href}>
  &larr; {label}
</a>
