---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"a"> & {
  href: string;
  label: string;
};

const { href, label, class: className, target = "_blank", rel, ...rest } = Astro.props as Props;

const resolvedRel = rel ?? (target === "_blank" ? "noopener noreferrer" : undefined);
---

<a
  href={href}
  target={target}
  rel={resolvedRel}
  class:list={[
    "group inline-flex items-center gap-1 rounded-lg border border-transparent text-base font-medium text-foreground no-underline hover:no-underline focus-visible:border-ring focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-ring/50",
    className,
  ]}
  {...rest}
>
  <slot />
  <span class="link-underline group-hover:after:scale-x-100 group-focus-visible:after:scale-x-100">
    {label}
  </span>
</a>
